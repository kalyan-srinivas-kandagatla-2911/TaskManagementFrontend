query getMe {
  getMe {
    username
    team
    email
    id
    offId
    role
  }
}

query GetTasksCreatedByMe($userId: String!) {
  getTasksCreatedByMe(user_id: $userId) {
    description
    id
    title
    updatedAt
    deadline
  }
}